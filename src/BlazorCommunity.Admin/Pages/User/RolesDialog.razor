@inherits RolesDialogBase
@using BlazorCommunity.DTO.Admin
已选中数据：@string.Join(",", SelectedValues.Select(p=>p.Name))
<br />


<ElForm Style="width:100%;" @ref="roleForm" Model="rolesDto" LabelPosition="@LabelAlign.Right" EnableAlwaysRender="true">
    <ElFormItem TValue="List<RoleDisplayDto>" Label="角色列表" Prop="@nameof(RolesDto.Roles)" EnableAlwaysRender="true">
        <ElCheckbox TValue="object" Model="true" Status="@Status" StatusChanged="status=>SelectAll(status)">全�</ElCheckbox>
        <ElCheckboxGroup TValue="List<RoleDisplayDto>" EnableAlwaysRender="true">
            @foreach (var item in Values)
            {
                <ElCheckbox TValue="object" Model="item.Id" EnableAlwaysRender="true" Status="SelectedValues.Contains(item) ? Status.Checked:Status.UnChecked" StatusChanged="status=> ChangeStatus(status,item)">@item.Name</ElCheckbox>
            }
        </ElCheckboxGroup>
    </ElFormItem>

    <ElFormItem TValue="object" Style="text-align:center;margin:0 auto;">
        <ElButton Type="@ButtonType.Primary" Plain="true" OnClick="e=>Save()" Style="text-align:center;width:100px;">保存</ElButton>
        <ElButton Type="@ButtonType.Default" Plain="true" OnClick="e=>roleForm.Reset()" Style="text-align:center;width:100px;">重置</ElButton>
    </ElFormItem>
</ElForm>
    @code {

    }

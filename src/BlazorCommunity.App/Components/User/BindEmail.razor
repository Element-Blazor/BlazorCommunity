@inherits BindEmailBase
<ElCard EnableAlwaysRender="true" @ref="bCard">
    <Body>
        @if (value != null && !string.IsNullOrWhiteSpace(value.Email))
        {
        <ElForm @ref="ElForm" Model="value" LabelPosition="right" Inline="true" EnableAlwaysRender="true">
            <ElFormItem TValue="string" Label="" Prop="@nameof(PasswordModel.Email)" Required="false">
                <ElButton Type="ButtonType.Info" Plain="true" Style="border:none" Disabled="true">@($"已绑定：{ value.Email}")</ElButton>
            </ElFormItem>
            @if (SendCanCelEmailBindCodeSuccess)
            {
                <ElFormItem TValue="string" Label="验证码：" Prop="@nameof(PasswordModel.Code)" Required="false">
                    <ElInput TValue="string" Placeholder="请输入验证码"></ElInput>
                </ElFormItem>
                <ElFormActionItem EnableAlwaysRender="true" Style="text-align-last:right;">
                    <ElButton Type="@ButtonType.Default" Disabled="BtnConfirmDisabled" Plain="true" OnClick="e => CanCelBindConfirm()" EnableAlwaysRender="true">
                        确定
                    </ElButton>
                </ElFormActionItem>
            }
            else
            {
                <ElFormActionItem EnableAlwaysRender="true" Style="text-align-last:right;">
                    <ElButton Type="@ButtonType.Info" Disabled="BtnCancelDisabled" Plain="true" OnClick="e => CanCelBind()" EnableAlwaysRender="true">
                        取消绑定
                    </ElButton>
                </ElFormActionItem>
            }

        </ElForm>
        }
        else
        {
            <ElForm @ref="ElForm" Model="value" LabelPosition="right" Inline="true" EnableAlwaysRender="true">
                <ElFormItem TValue="string" Label="邮箱�? Prop="@nameof(PasswordModel.Email)" Required="true">
                    <ElInput TValue="string" Type="@InputType.Tel" Placeholder="请输入邮箱地址"></ElInput>
                </ElFormItem>
                <ElFormActionItem EnableAlwaysRender="true" Style="text-align-last:right;">
                    <ElButton Type="@ButtonType.Default" Plain="true" OnClick="e => SendEmailMsg()" EnableAlwaysRender="true" Disabled="BtnBindEmailDisabled">
                        @((TimeOut > 0 && TimeOut < CountDownTime)?$"请等待{TimeOut}s后重�?:"绑定邮箱")
                    </ElButton>
                </ElFormActionItem>
                @if (showInput)
                {
                    <ElFormItem TValue="string" Label="验证码：" Prop="@nameof(PasswordModel.Code)" Required="false">
                        <ElInput TValue="string" Placeholder="请输入验证码"></ElInput>
                    </ElFormItem>
                    <ElFormActionItem EnableAlwaysRender="true" Style="text-align-last:right;">
                        <ElButton Type="@ButtonType.Default" Plain="true" OnClick="e => CheckVerifyCode()" EnableAlwaysRender="true">
                            确定
                        </ElButton>
                    </ElFormActionItem>
                }
            </ElForm>
        }
    </Body>
</ElCard>
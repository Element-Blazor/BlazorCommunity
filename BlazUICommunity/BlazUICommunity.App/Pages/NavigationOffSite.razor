@inherits PageBase
@page "/navigate"
@page "/navigate/{Route}"

@code {

    [Parameter]
    public string Route { get; set; }

    protected override async Task OnInitializedAsync()
    {
        if (!string.IsNullOrEmpty(Route))
            navigationManager.NavigateTo($"/navto/{Route}", true);
        else
            navigationManager.NavigateTo("/", true);
    }
}